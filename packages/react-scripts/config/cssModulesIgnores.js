// Ignored CSS module packages that doesn't fit the opinionated
// modules-first setup. These packages will be processed with the same
// pipeline CSS modules files are processed, but with `modules: false`.
module.exports = [
  /.*react-dates.*\.css/,
];
